apiVersion: v1
kind: Service
metadata:
  name: {{ include "floodgate.fullname" . }}
  labels:
    {{- include "floodgate.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "floodgate.selectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: {{ .Values.config.grpc_port }}
      targetPort: grpc
      protocol: TCP
