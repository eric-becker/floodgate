This directory should contain the Meshtastic protobuf definitions.

To download them, run from the project root:

```bash
./scripts/download_protobufs.sh
```

This clones https://github.com/meshtastic/protobufs into this directory.
The contents are git-ignored since they are fetched at build time.
